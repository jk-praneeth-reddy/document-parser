NODE_ENV=development
PORT=8000

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# OpenAI — required for OCR extraction
OPENAI_API_KEY=sk-...
