# Production: full origin of your deployed backend (no trailing slash).
# Example: https://parser-backend.onrender.com
# Leave unset for local dev — Vite proxies /api to localhost (see vite.config.ts).
# VITE_API_BASE_URL=https://parser-backend.onrender.com
